import Foundation

/// An enumeration that defines alignment options for a component.
public enum ComponentAlignment: Hashable {
  /// The alignment options for a component.
  case fill
  
  /// The alignment options for a component.
  case leading
  
  /// The alignment options for a component.
  case trailing
  
  /// The alignment options for a component.
  case center
  
  /// The alignment options for a component.
  case top
  
  /// The alignment options for a component.
  case bottom
  
  /// The alignment options for a component.
  case topLeading
  
  /// The alignment options for a component.
  case topTrailing
  
  /// The alignment options for a component.
  case bottomLeading
  
  /// The alignment options for a component.
  case bottomTrailing
}
